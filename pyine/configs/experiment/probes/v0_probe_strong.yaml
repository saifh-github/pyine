# @package _global_
#
# Probe training experiment with strong resampling bias — inherits from v0_probe_moderate
#
# Usage:
#   Single GPU:  python -m pyine.apps.trainers.probe_trainer +experiment=probes/v0_probe_strong
#   Multi-GPU:   bash scripts/run_ddp.sh --app pyine.apps.trainers.probe_trainer -- +experiment=probes/v0_probe_strong

defaults:
  - v0_probe_moderate
  - override /config/datamodule_config/resampling: skewed_strong_bias
  - override /config/evals_config/calibration_resampling: skewed_strong_bias
  - _self_

runtime:
  run_name: "${now:%Y%m%d_%H%M%S}_skewed_strong_bias"
